<p id="notice"><%= notice %></p>

<h1>Catalogues</h1>

<table>
  <thead>
    <tr>
      <th>Clase</th>
      <th>Descripcion</th>
      <th>Orden</th>
      <th>Admin user</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @catalogues.each do |catalogue| %>
      <tr>
        <td><%= catalogue.clase %></td>
        <td><%= catalogue.descripcion %></td>
        <td><%= catalogue.orden %></td>
        <td><%= catalogue.admin_user %></td>
        <td><%= link_to 'Show', catalogue %></td>
        <td><%= link_to 'Edit', edit_catalogue_path(catalogue) %></td>
        <td><%= link_to 'Destroy', catalogue, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Catalogue', new_catalogue_path %>
