json.extract! formula, :id, :product_id, :nombre, :descripcion, :string, :admin_user_id, :descripcion, :string, :orden, :obs, :cantidad, :numero, :pfecha, :created_at, :updated_at
json.url formula_url(formula, format: :json)
